spring:
  datasource:
    url: jdbc:postgresql://postgres:6432/persondb
    username: myuser
    driverClassName: org.postgresql.Driver
    hikari:
      maximum-pool-size: 1
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect